import CartWidget from "./CartWidget";

function NavBar() {
    return (
        <nav>
            <h1>Mi E-Commerce
            </h1>
            <CartWidget/>        
        </nav>
        );
}

export default NavBar;